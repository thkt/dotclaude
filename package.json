{
  "name": "dotclaude",
  "version": "0.0.0",
  "private": true,
  "description": "Harness config repo. oxlint/oxfmt pinned so the Zed oxc extension uses these local binaries instead of fetching latest (blocked by min-release-age).",
  "devDependencies": {
    "oxfmt": "0.56.0",
    "oxlint": "1.71.0"
  }
}
