# wiki README テンプレート

/scribe が Phase 1 で、`docs/wiki/README.md` が無いときに以下の内容をそのまま書き出す。置き換える箇所は無い。

## Template

```markdown
# docs/wiki

このプロジェクトで繰り返し現れる定型手順 / 規約と再発指摘を、過去の PR / issue から蒸留して蓄積する場所。issue の Plan やレビューで毎回同じ説明 / 同じ指摘を繰り返す代わりに、ここ 1 箇所を参照して二重管理と陳腐化を防ぐ。

1 共通項 1 ページで、各ページは 内容 → 定型手順 → 参照コード → 由来 → 根拠 の順。由来は共通項が派生した `docs/decisions/` の DR への参照で、その DR が supersede されたらページの書き換えが必要になる密接なものだけ張られる。根拠が 1 件だけの共通項は `_candidates.md` に置かれ、2 件目が現れたらページへ昇格する。初出でも根拠が 2 件揃っていれば直接ページになる。1 回の run が動かすページ数には上限があり、昇格できるのに溢れた分は `_candidates.md` の昇格待ちへ回って次の run で先に処理される。

`kind: structure` を持つページは共通項ではなく、モジュールの構造を書く。節は 内容 → 境界 → 契約 → 要求 → 参照コード → 由来 で、定型手順 と 根拠 は持たない。根拠 2 件の閾値は掛からず、手で書いて足す。共通項ページと同じ glob を持ってよいが、共通項ページが述べた内容は書かずページ名で参照する。

## 育て方

`/scribe` スキルまたは定期巡回の scribe が、closed になった PR / issue から自動で抽出する。初回は全履歴を読んで基礎化し、以降は前回の scribe PR マージ以降に closed になった分から増分更新する。追加 / 修正は PR で提案されるので、レビューしてマージする。マージが承認になる。手動追記も可。

## 調査原本

調査原本は `docs/research/` に置き、根拠から `[research:name.md](../research/name.md)` で辿れるようにする。観測日・版・条件・未確認事項を保持する。旧 `(research)` の原本は推測で埋めない。同じ原本の別配置や改訂を独立した根拠に数えない。手動 `/scribe` は研究だけの追加・更新も読み、自動起動の条件は変えない。

```
