# Research: sae-100-adr-0060-prep

Generated: 2026-05-11
Session: c087504c-a002-40bd-8cdc-426f47b5e43d
Intent: Feature planning
Domain: General (CLI tooling, cross-repo)
Prior research: `agent-friendly-cli-audit.md` (2026-03-27), `0060-adopt-agent-friendly-cli-design-principles.md` (ADR)

## Purpose

Investigate sae #100 (ADR-0060 適用) の事前材料を集める。issue コメントに含めるべき事実、横並びチェック (recall #67 / yomu #156)、amici uplift 必要性、Phase 計画の検証材料を抽出する。実装は本調査では行わない。

## Key Findings

| Priority | Finding | Source | Next Action |
| --- | --- | --- | --- |
| 1 | sae 既に global `--json` 実装済み。`Cli::json: bool` (global = true), `cli.json` を全 subcommand に貫流 | `sae/src/main.rs:22, 184-214` | 監査チェックリストの「グローバル `--json`」は **半分済** (フラグ実装) / **半分未** (envelope shape 不在) と区別記述 |
| 1 | sae error 経路は `--json` 指定でも `error: <text>` を stderr に書くのみ。JSON envelope, `next_step`, `candidates` 全て不在 | `sae/src/main.rs:236-251` (exit_error), `sae/src/config.rs:62, 132-151`, `grep` 結果 (next_step/candidates 該当ゼロ) | 監査タスク "エラー JSON に next_step + candidates" は最優先。`ConfigError::UnknownTeam(String)` を `{name, known: Vec<String>}` 構造に拡張する設計 |
| 1 | sae stdout への panic on BrokenPipe 再現。**ただし `| head -1` ではない**: human mode 1 行は単一 write で完結するため発火しない。`| head -0` / `| true` (stdout を即時クローズ) で確実に発火 | 実測: `/tmp/sae-err.log` に `thread 'main' panicked at .../io/stdio.rs:1165:9: failed printing to stdout: Broken pipe (os error 32)`; `grep BrokenPipe sae/amici/src/` 0 件 | 監査タスク "BrokenPipe handling" 必要。issue コメントに正確な再現条件を記載 (head -1 の誤情報を避ける) |
| 1 | amici `codes` モジュールは ADR-0060 が要求する 5 値のうち **65 EX_DATAERR と 66 EX_NOINPUT を欠く**。ADR では `0/64/65/66/74/75`、amici は `0/64/70/73/74/75` | `amici/src/cli/exit_code.rs:56-70` | sae 着手前に amici に `codes::DATA_ERR (65)`, `codes::NO_INPUT (66)` を追加する prerequisite PR が必要 |
| 1 | amici には **JSON envelope 型も write_output ヘルパーも存在しない**。BrokenPipe 専用処理もゼロ。scout が持つ `CommandOutput`, `SuccessEnvelope`, `ErrorEnvelope`, `ErrorPayload`, `ErrorCode`, `write_output` は scout-internal (`pub(crate)`) | `scout/src/envelope.rs:13-92`, `scout/src/lib.rs:24-30, 112-134, 169-177`; cross-verified: `grep -rn write_output` 0 件 (amici/sae/yomu/recall), amici cli `pub` 一覧 (message/spinner/config/shorthand のみ) | scout の `envelope.rs` + `write_output` + `handle_parse_error` + `emit_error` を **設計の出発点として amici::cli へ移植** (ADR-0065 がスキーマを fix 済み) |
| 1 | 並行 issue は **既に立っている**。`recall #67` (OPEN) と `yomu #156` (OPEN) は sae #100 と body **完全一致** (Supersedes 行のみ差分: sae #100→#2, yomu #156→#10, recall #67→なし) | `gh issue view 100 / 67 / 156` の body 比較 | 「並行 issue 化候補」タスクは moot。issue コメントは「3 件同時進行 / 共通先行 (amici uplift) を提案」に reframe |
| 2 | ADR-0060 reassessment trigger ("既存 CLI 3 つ以上が 3 軸を満たした後") に向けた現在地スコア (3 軸 = 機械可読 / 次の手 / 自己発見) | 下記 "3-Axis Snapshot Matrix" 表参照 | sae #100 解決のみでは scout (3/3) + sae (推定 2/3) で **2 ツール**。yomu / recall 完了で 3+ 達成 |
| 2 | sae `ConfigError::UnknownTeam` の error 構築点で **`Config.teams: Vec<String>` が in-scope**。`candidates` field の supplier として最も自然 | `sae/src/config.rs:58-66` (`resolve_team`), `ConfigError::UnknownTeam(t.to_owned())` 行 | variant signature を `UnknownTeam { name: String, known: Vec<String> }` に拡張し、JSON 化時に `candidates` フィールドへ流す |
| 2 | sae search/post の Input エラー 5 種類は **テキストに next_step を既に含む**。JSON 化時はテキストを分割して `message` + `next_step` 分割が必要 | `sae/src/commands/search.rs:27, 172, 190-194`, `sae/src/tools.rs:290, 339` | テキストの自然分割パターンを抽出 (例: `"... first."` `"... or use ..."`) |
| 2 | sae はすでに `IsTerminal` + `-` パターン実装済み (`commands/search.rs:2, 202`)。Shorthand expansion + OSA 距離 typo guard も実装済み (`main.rs:170-181`) | `sae/src/commands/search.rs:197-206`, `sae/src/main.rs:170-181` | 監査チェックリストの "stdin 統一", "Shorthand expansion + safety rail" は **既に Pass**。issue コメントで明示 |
| 2 | sae `--dry-run` は create / update / archive / ship に実装済み (clap `dry_run: bool`) | `sae/src/tools.rs:67, 94`, `sae/src/main.rs:103, 118` | "推奨 (補強)" の `--dry-run` 項目も Pass 済み |
| 3 | yomu は `degraded`/`notes` を成功 JSON に出力済み (`format_results_json(results, degraded, notes)`)。ただし **error 経路は sae と同じ平文** (`error: <text>`)、`next_step`/`candidates` ゼロ | `yomu/src/tools.rs:299`, 実測 `yomu search` (no arg) → `error: empty query from stdin` exit 0 | yomu #156 着手時、error JSON envelope は sae と並列で進む必要がある |
| 3 | recall は global `--json` が **未実装** (`error: unexpected argument '--json' found`) | 実測 `/Users/thkt/GitHub/cli/recall/target/debug/recall --json search foo` | recall #67 は 3 軸の機械可読軸からゼロから着手 |
| 3 | sae search の success JSON shape は `{results, search_mode}`、status は `[TeamStatus...]`、get は post object — 各コマンドが異なる top-level shape。envelope なし | `sae/src/output.rs:24-71, 73-87, 167-210` | 監査タスク "グローバル --json + serde + stdout/stderr 分離" の **本質的未実装部分** は envelope (data/degraded/notes wrapper) であって flag ではない |
| 3 | sae 既存テストが `--json` global flag を validate 済み (T-004) | `sae/src/main.rs:539-548` (`fn model_download_with_json_flag`) | テスト再利用可能 |

## Available Data

### Source files

| Type | Item | Note |
| --- | --- | --- |
| File | `sae/src/main.rs` | CLI entry, `--json` global, shorthand expansion |
| File | `sae/src/tools.rs` | `SaeError` enum, `CliError impl` (sysexits routing), `SearchArgs/CreateArgs/UpdateArgs` |
| File | `sae/src/commands/search.rs` | `parse_date_arg` (Invalid date), `resolve_value_with_reader` (IsTerminal + `-`) |
| File | `sae/src/config.rs` | `ConfigError::UnknownTeam(String)`, `Config.teams: Vec<String>` (candidates supplier) |
| File | `sae/src/output.rs` | per-command JSON shapes (no envelope) |
| File | `amici/src/cli/exit_code.rs` | `codes::{SUCCESS, USAGE, SOFTWARE, CANT_CREAT, IO_ERR, TEMP_FAIL}` — missing DATA_ERR(65), NO_INPUT(66) |
| File | `amici/src/cli/message.rs` | `exit_error`, `hint_arrow`, `info`, `deprecation_warn`, `progress_step` |
| File | `amici/src/cli.rs` | public re-exports of cli helpers |
| File | `scout/src/envelope.rs` | `CommandOutput`, `SuccessEnvelope`, `ErrorEnvelope`, `ErrorPayload`, `ErrorCode` (all `pub(crate)`) |
| File | `scout/src/lib.rs:24-30, 60-177` | `write_output`, `handle_parse_error`, `emit_error`, `render_json_success/error`, pre-scan argv for `--json` |
| Doc | `.claude/docs/decisions/0060-...md` | ADR text + 3-axis + reassessment trigger |
| Doc | `.claude/docs/decisions/0065-scout-json-output-schema-...md` | envelope schema + sysexits table (1:1 mapping) |

### Issues

| Repo | Number | State | Body identity vs sae #100 |
| --- | --- | --- | --- |
| sae | #100 | OPEN | (本 issue) |
| sae | #2 | CLOSED | Eric Zakariasson 基準。Supersedes 元 |
| recall | #67 | OPEN | **完全一致** (Supersedes 行なし) |
| recall | #10 | OPEN | Eric Zakariasson 基準 (recall 個別) |
| yomu | #156 | OPEN | **完全一致** (Supersedes #10) |
| yomu | #10 | CLOSED | Eric Zakariasson 基準 |

### Tech / convention

| Type | Item | Note |
| --- | --- | --- |
| Tech | `clap derive` v4 | `#[arg(long, global = true)]` パターン |
| Tech | `thiserror` | `SaeError`, `ConfigError`, `ClientError`, `StorageError`, `SyncError`, `ModelDownloadError` の階層 |
| Tech | `serde_json::Value` | scout envelope payload。命令型 (per-command) と structural (envelope) を組み合わせる |
| Convention | sysexits.h | scout PR #74-77 + ADR-0065 で確定 (0/64/65/66/74/75) |
| Convention | clap `after_help` Examples | sae 全 subcommand 実装済み (`main.rs:31-150`) |
| Convention | IsTerminal + `-` | sae/yomu 実装済み。recall は show/status のみ |

## Constraints

| Category | Constraint |
| --- | --- |
| Compat | ADR-0060 reassessment は "3 ツール以上が 3 軸を満たした" 時点で発火。scout 完了済 (3/3) → あと 2 つ要 |
| Compat | sae の現 success JSON shape (`{results, search_mode}` 等) は **既に外部観測可能**。envelope 化は **破壊的変更** (scout は v0.8.x で minor bump、ADR-0065 §Migration Strategy) |
| Compat | amici は CLI 3 ツールが依存する共有 crate。`codes::DATA_ERR / NO_INPUT` 追加は **後方互換** (公開 const 増分) だが、`envelope` 型新設は新 module |
| Order | amici uplift (codes 拡張 + envelope module + write_output helper) を **先行 PR** にすると、3 issue (sae #100 / recall #67 / yomu #156) が並列で envelope import 可能 |
| Security | `--json` error 経路で stack trace / internal path を漏らさない (`SaeError::Io`, `SaeError::Other` に file path が embed されている箇所がある: 例 `config.rs:140` の `ReadFailed(PathBuf, io::Error)`) |

## Disconfirmation Check

Phase 3 (Bug Strong Inference) は適用外 (Intent = Feature planning)。Phase 2 audit trail から重要な "absence" claim をここで verbatim 引用する。

### Claim 1: amici には write_output / envelope 型がない

Command:
```
grep -rn '\bwrite_output\b' /Users/thkt/GitHub/cli/amici/src/ /Users/thkt/GitHub/cli/sae/src/ /Users/thkt/GitHub/cli/yomu/src/ /Users/thkt/GitHub/cli/recall/src/
```

Raw output:
```
(no matches)
```

Cross-method verify:
1. `grep -rn 'pub fn\|pub struct\|pub enum\|pub use' /Users/thkt/GitHub/cli/amici/src/cli/` → message/spinner/config/shorthand/exit_code のみ (envelope 該当ゼロ)
2. `grep -rn 'envelope\|SuccessEnvelope\|ErrorEnvelope\|ErrorPayload' /Users/thkt/GitHub/cli/amici/src/ /Users/thkt/GitHub/cli/sae/src/` → amici 側ヒットは `eval_harness.rs` の `BaselineSnapshot` envelope (別概念)、`annotation.rs:9` の Provenance envelope (別概念)。CLI 出力 envelope に該当する型は **ゼロ**

Result: amici に CLI envelope / write_output helper は存在しない。scout の実装を出発点として amici::cli::envelope module (新規) + amici::cli::write_output (新規) を作る前提が成立。

### Claim 2: sae に BrokenPipe ハンドリングがない

Command:
```
grep -rn 'BrokenPipe\|broken_pipe' /Users/thkt/GitHub/cli/sae/src/ /Users/thkt/GitHub/cli/amici/src/
```

Raw output:
```
(no matches)
```

Cross-method verify (runtime probe):
```
/Users/thkt/GitHub/cli/sae/target/debug/sae --json search foo 2>/tmp/sae-err.log | true; cat /tmp/sae-err.log
```
出力:
```
thread 'main' (8318549) panicked at /rustc/.../io/stdio.rs:1165:9:
failed printing to stdout: Broken pipe (os error 32)
note: run with `RUST_BACKTRACE=1` environment variable to display a backtrace
```

Result: ソースに該当処理なし + 実行時に panic 再現。`| head -1` では発火しない (human mode 1 行は単一 write、buffered stdout が flush 失敗するのは `| head -0` / `| true` / 即時クローズ系シナリオ)。issue コメントに正確な再現条件を記す。

### Claim 3: 並行 issue は既に存在 (重複 issue 化を避ける)

Command:
```
gh issue view 100 --json body --jq .body    # sae
cd recall && gh issue view 67 --json body --jq .body
cd yomu && gh issue view 156 --json body --jq .body
```

Raw output: body は 3 件とも **同一テキスト** (Supersedes 行の参照番号のみ差分)。sae #100 / recall #67 / yomu #156 が同一テンプレで先に切られている。

Result: 「並行 issue 化候補」タスクは moot。3 件並走を前提に、共通先行 (amici uplift) を提案するフレームに reframe する。

## 3-Axis Snapshot Matrix (ADR-0060 reassessment trigger)

成功軸 = ADR-0060 §3 軸 (機械可読 / 次の手 / 自己発見)。判定基準は ADR の "必須実装" 列。

| CLI | 機械可読 (`--json` + serde + stdout/stderr 分離) | 次の手 (sysexits + error JSON next_step+candidates) | 自己発見 (Noun-Verb + parse-time conflicts_with) | 結論 |
| --- | --- | --- | --- | --- |
| **scout** | Pass (envelope, ADR-0065) | Pass (envelope に next_step/candidates) | Pass (Noun-Verb 構造、clap) | **3/3 ✅** |
| **sae** | Half (`--json` flag + per-command shape, **envelope なし**) | Half (sysexits Pass、error JSON 不在: next_step/candidates ゼロ) | Pass (`harvest/search/get/create/...`, conflicts_with 実装: `body`/`body_file`) | **1.5/3** |
| **yomu** | Half (`--json` flag + `degraded`/`notes` あり、**envelope なし**) | Half (sysexits Pass、error JSON 不在: 平文 `error: ...`) | Pass (`search/index/rebuild/impact/...`、conflicts_with 実装) | **2/3** |
| **recall** | Fail (`--json` 未実装: `unexpected argument`) | Half (USER_ERROR_MARKERS による UE/SE 分離あるが sysexits 完全ではない、`bail!`ベース) | Pass (`index/embed/search/show/status`、サブコマンド構造) | **1.5/3** |

**Reassessment trigger は scout (3/3) + あと 2 ツールが 3/3 達成で発火**。sae #100 解決のみでは scout + sae = 2 ツール。yomu #156 か recall #67 のいずれかが揃った時点で trigger 発火。

実務的 implication: sae 単独完了より **3 issue を並走させて共通 amici uplift を先行** させる方が trigger 到達コストが安い。

## References

| Path | Description |
| --- | --- |
| `.claude/workspace/research/agent-friendly-cli-audit.md` | Eric Zakariasson 基準の過去監査 (2026-03-27) baseline |
| `.claude/docs/decisions/0060-adopt-agent-friendly-cli-design-principles.md` | ADR-0060 (本 issue の根拠) |
| `.claude/docs/decisions/0065-scout-json-output-schema-and-sysexits-exit-code-policy.md` | scout envelope schema 確定 ADR |
| `/Users/thkt/GitHub/cli/scout/src/envelope.rs` | envelope 型のリファレンス実装 |
| `/Users/thkt/GitHub/cli/scout/src/lib.rs` | write_output + emit_error + handle_parse_error のリファレンス |
| `/Users/thkt/GitHub/cli/sae/src/main.rs:22, 184-251` | sae `--json` flag + main loop |
| `/Users/thkt/GitHub/cli/sae/src/tools.rs:254-334` | `SaeError` + `CliError impl` (sysexits routing) |
| `/Users/thkt/GitHub/cli/sae/src/commands/search.rs:18-32, 162-206` | `parse_date_arg`, `resolve_search_query` (stdin) |
| `/Users/thkt/GitHub/cli/sae/src/config.rs:58-72, 132-151` | `ConfigError::UnknownTeam`, `Config.teams: Vec<String>` |
| `/Users/thkt/GitHub/cli/sae/src/output.rs` | per-command JSON shapes (envelope なし) |
| `/Users/thkt/GitHub/cli/amici/src/cli/exit_code.rs:56-70` | amici sysexits `codes` (DATA_ERR/NO_INPUT 欠) |
| `/Users/thkt/GitHub/cli/amici/src/cli.rs` | amici cli public surface (message/spinner/config/shorthand/exit_code) |
| `https://github.com/thkt/sae/issues/100` | 本 issue |
| `https://github.com/thkt/recall/issues/67` | 並行 issue (recall) |
| `https://github.com/thkt/yomu/issues/156` | 並行 issue (yomu) |
| `https://github.com/thkt/scout/pull/74-77` | scout BrokenPipe / envelope / `--json` wiring / sysexits PR シリーズ |

## Coverage Notes

- Phase 1 question (1) sae 実動作確認: 全 sub-question 解消済 (success JSON shape, error path text-fallback, BrokenPipe 再現条件)
- Phase 1 question (2) 横並びチェック: recall #67 / yomu #156 既存。body 完全一致 (3 件 verbatim 比較で確認)。envelope 共通化は **amici uplift を共通先行 PR にする提案** で扱う
- Phase 1 question (3) amici ヘルパー在庫: `exit_error/hint_arrow/info/deprecation_warn/progress_step/Spinner/embed_with_spinners/with_spinner/done/env_lookup/try_expand_shorthand/CliError/codes::{SUCCESS,USAGE,SOFTWARE,CANT_CREAT,IO_ERR,TEMP_FAIL}` のみ。`write_output` / envelope 型は **不在** (cross-verify 3 method)
- Phase 1 question (4) ConfigError::UnknownTeam: 既知 team 一覧は `Config.teams: Vec<String>` (config.rs:11)。construction 時点で in-scope。`{name, known: Vec<String>}` 拡張で `candidates` 供給可
- Phase 1 question (5) reassessment trigger: scout (3/3) のみ。sae/yomu/recall は 1.5-2/3 で **2 ツール目候補は yomu** (`degraded`/`notes` 既に出力済みで envelope 化のみが残)
- Tool disagreement (Phase 2): なし。runtime probe / source grep / yomu semantic search 全 method で一致
- Advisor (Phase 4): 6 つの sharpening 指摘を受領済。BrokenPipe 再現条件 (item 1)、`--json` 半分済の framing 分離 (item 2)、amici 65/66 欠落 surface (item 3)、並行 issue moot (item 4)、Config.teams 構造拡張ポインタ (item 5)、3 軸数値化 (item 6)、issue body diff (item verify) を全て反映

## Next Steps

| Intent | Next Command |
| --- | --- |
| Feature planning | `/think` (issue #100 コメント本文 + Phase 計画の SOW / Spec 生成。先行 amici PR の必要性 + 3 軸 reassessment trigger 計算根拠を含めて planning) |
