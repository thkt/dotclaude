# SPEC

📌 **[日本語版](../.ja/docs/SPEC.md)**

This document collects the execution contract of every harness component in one place: inputs, outputs, stop conditions, and behavior on failure. It does not repeat design intent. `settings.json` is the source of truth for what is actually registered. The wiring table here copies it, with one exception: a script registered twice, once for `Write` and once for `Edit`, takes a single row.

## Where to look

The document you want depends on the question. This one answers what happens; the others answer why it was built that way.

| Question                                  | Where                                       |
| ----------------------------------------- | ------------------------------------------- |
| Contract (input, output, stop, failure)   | This document                               |
| Design intent of the layer structure      | [DESIGN.md](./DESIGN.md)                    |
| Design intent and naming of hooks         | [HOOKS.md](./HOOKS.md)                      |
| Skills versus Agents                      | [SKILLS_AGENTS.md](./SKILLS_AGENTS.md)      |
| How one decision was reached              | [decisions](./decisions)                    |
| Role of each external CLI                 | [CLI_TOOLS.md](./CLI_TOOLS.md)              |
| Term definitions                          | [GLOSSARY.md](./GLOSSARY.md)                |

## System boundary

This repository holds scripts and Markdown, nothing else. Most binaries invoked at runtime live outside it, and a hook whose binary is not installed exits without doing anything.

| Component                                   | Implementation                          | Supplied by                     |
| ------------------------------------------- | --------------------------------------- | ------------------------------- |
| Hook scripts                                | `hooks/**/*.py`, `hooks/**/*.sh`        | This repository                 |
| Skill / agent / workflow definitions        | `skills/`, `agents/`, `workflows/`      | This repository                 |
| `guardrails`, `formatter`, `gates`, `assay` | Rust binaries                           | `brew install thkt/tap/{tool}`  |
| `scout`, `recall`, `sae`, `xr`              | Rust CLIs                               | `brew install thkt/tap/{tool}`  |
| `ast-grep`                                  | Rust binary                             | `brew install ast-grep`         |
| `codegraph`                                 | Binary behind a mise shim               | mise                            |
| `hooks/herdr-agent-state.sh`                | File generated by herdr                 | herdr (reinstall overwrites it) |
| `textlint`, `oxlint`, `oxfmt`               | devDependencies                         | `bun install`                   |
| `ruff`, `rumdl`                             | Python linters                          | pipx (CI pins the versions)     |
| Amphetamine                                 | macOS app                               | External app                    |

## Components and responsibilities

The directory is the unit of responsibility. The runner column names who reads the file and drives it.

| Directory     | Responsibility                             | Runner               |
| ------------- | ------------------------------------------ | -------------------- |
| `rules/`      | Conventions and principles; `paths:` frontmatter decides which files load | Claude itself        |
| `skills/`     | Procedure and knowledge modules            | Skill tool / model   |
| `agents/`     | Specialist definitions run in a fork       | Agent tool           |
| `workflows/`  | Deterministic orchestration scripts        | Workflow tool        |
| `hooks/`      | Scripts that interrupt a tool call         | Claude Code harness  |
| `docs/`       | Human-facing design docs and decisions     | Humans               |
| `tests/`      | Cross-cutting tests that enforce rules     | `node --test`        |

## Hook contract

### Input and output

A hook reads a JSON payload from stdin and writes JSON, or text destined for additionalContext, to stdout. Every hook exits 0. A non-zero status is a broken hook rather than a designed result, and `checked` in `hooks/_lib/hook_harness.py` raises AssertionError on it. That check covers the 12 Python hooks started through the harness; the 2 shell hooks are tested through their own scripts, which read the result alone.

A hook that stops a call goes through `hook_payload.deny` and emits the same envelope. `permissionDecision` takes exactly four values: `allow`, `deny`, `ask`, `defer`. Anything else fails schema validation, and the gate then passes the call silently while stopping nothing.

| Output              | Shape                                                                                 |
| ------------------- | ------------------------------------------------------------------------------------- |
| Refusal (PreToolUse)| `{"hookSpecificOutput":{"hookEventName","permissionDecision","permissionDecisionReason"}}` |
| Context injection   | JSON carrying `additionalContext`, or plain text                                      |
| No action           | Empty output and `exit 0`                                                             |

### Wiring

The order below is the registration order in `settings.json`. Within one matcher the hooks run top to bottom, and a hook whose `if` condition does not hold never starts. Each of the four scripts carrying an `if` condition is registered twice, once for `Write` and once for `Edit`, and the table folds that pair into one row. `EnterPlanMode` and `WebFetch` / `WebSearch` have no script: the registration echoes a deny envelope directly, and the reason names the replacement (`/think` and the scout CLI).

| Event             | Matcher            | Implementation                                  | if condition       | timeout |
| ----------------- | ------------------ | ----------------------------------------------- | ------------------ | ------- |
| PreToolUse        | `Bash`             | `pre-bash/package_manager_rewrite.py`           | none               | 15      |
| PreToolUse        | `Bash`             | `security/npm_install_guard.py`                 | none               | 60      |
| PreToolUse        | `Bash`             | `security/rm_to_trash.py`                       | none               | 15      |
| PreToolUse        | `Bash`             | `security/git_sandbox_guard.py`                 | none               | 15      |
| PreToolUse        | `Bash`             | `pre-bash/body_proofread.py`                    | none               | 60      |
| PreToolUse        | `Bash`             | `pre-bash/issue_body_gate.py`                   | none               | 30      |
| PreToolUse        | `Bash`             | `pre-bash/client_identifier_gate.py`            | none               | 30      |
| PreToolUse        | `Write\|Edit`      | `edit/rust_pre_edit.py`                         | `**/*.rs`          | 60      |
| PreToolUse        | `Write\|Edit`      | `guardrails`                                    | none               | 30      |
| PreToolUse        | `EnterPlanMode`    | Inline `echo` deny                              | none               | none    |
| PreToolUse        | `WebFetch\|WebSearch` | Inline `echo` deny                           | none               | none    |
| PostToolUse       | `Write\|Edit`      | `edit/rust_post_edit.py`                        | `**/*.rs`          | 30      |
| PostToolUse       | `Write\|Edit`      | `edit/textlint_fix.py`                          | `**/*.md`          | 60      |
| PostToolUse       | `Write\|Edit`      | `edit/mirror_prose_guard.py`                    | `**/.ja/**`        | 10      |
| PostToolUse       | `Write\|Edit`      | `assay`                                         | none               | 30      |
| PostToolUse       | `Write\|Edit`      | `formatter`                                     | none               | 30      |
| PostToolUse       | `Write\|Edit`      | `gates`                                         | none               | 120     |
| PostToolUse       | `Bash`             | `gates changed`                                 | none               | 120     |
| PostToolUse       | `*`                | `integrations/amphetamine_agent_session.py background` | none         | 15      |
| SessionStart      | `*`                | `lifecycle/recall_index.ts`                     | none               | 60      |
| SessionStart      | `*`                | `herdr-agent-state.sh session`                  | none               | 10      |
| UserPromptSubmit  | none               | `integrations/amphetamine_agent_session.py acquire` | none           | 15      |
| UserPromptSubmit  | none               | `codegraph prompt-hook`                         | none               | 10      |
| Stop              | none               | `lifecycle/failure-alert.sh stop`               | none               | 60      |
| Stop              | none               | `integrations/amphetamine_agent_session.py release` | none           | 15      |
| StopFailure       | none               | `lifecycle/failure-alert.sh fail`               | none               | 60      |

### Per-hook decision and failure mode

fail-close refuses input it cannot judge, advisory always decides allow and returns guidance alone, and fail-open keeps the feature working when the hook itself fails.

| Hook                            | Fires on                | Decision                                                          | Failure mode |
| ------------------------------- | ----------------------- | ----------------------------------------------------------------- | ------------ |
| `rm_to_trash.py`                | Bash                    | Denies `rm` / `rmdir` / `unlink` / `shred` plus `find -delete` / `git clean`, and points at `mv ~/.Trash/` | fail-close |
| `npm_install_guard.py`          | Bash                    | Denies an install with `ignore-scripts` unset. The ni aliases count the same | fail-close |
| `git_sandbox_guard.py`          | Bash                    | Denies a tree-rewriting git call running sandboxed                | fail-close   |
| `package_manager_rewrite.py`    | Bash                    | Rewrites a package manager command into its ni equivalent, always deciding allow | advisory |
| `body_proofread.py`             | Bash                    | Proofreads a gh filing or commit body and returns it as additionalContext | advisory |
| `issue_body_gate.py`            | Bash                    | Matches a `gh issue create` body against its title's template, denying both a divergence and a state it cannot compare | fail-close |
| `client_identifier_gate.py`     | Bash                    | Denies a commit in this repository whose staged diff adds a term from the identifier list kept outside it | fail-close |
| `rust_pre_edit.py`              | Write / Edit (`*.rs`)   | Injects clippy findings as additionalContext                      | advisory     |
| `rust_post_edit.py`             | Write / Edit (`*.rs`)   | Runs `cargo fmt`, then clippy again, and returns the findings     | advisory     |
| `textlint_fix.py`               | Write / Edit (`*.md`)   | Auto-fixes a Markdown file that passes the Japanese check         | advisory     |
| `mirror_prose_guard.py`         | Write / Edit (`.ja/**`) | Warns about a `.ja/` file with no Japanese character. Never blocks | advisory    |
| `amphetamine_agent_session.py`  | UserPromptSubmit / PostToolUse / Stop | Holds the Mac awake through a reference count per session_id | fail-open |
| `recall_index.ts`               | SessionStart            | Catches recall's cross-session index up in the background         | fail-open    |
| `failure-alert.sh`              | Stop / StopFailure      | Sounds a turn that ended as anything but `end_turn`. Subagents are out of scope | fail-open |
| `statusline.sh`                 | `statusLine` key        | Renders the model name and usage. Partial display is accepted. It is registered under the top-level `statusLine` key, not in the `hooks` map | fail-open |
| `herdr-agent-state.sh`          | SessionStart            | Reports state only when herdr's environment variables are all set | fail-open    |

`hooks/_lib/` is where a piece of knowledge lives when more than one hook needs it.

| Module             | Knowledge it owns                                               |
| ------------------ | --------------------------------------------------------------- |
| `command_scan.py`  | Which token on a Bash line sits in command position, after stripping wrappers and assignments |
| `gh_filing.py`     | The body-flag spellings of `gh issue create` / `gh pr create`    |
| `hook_payload.py`  | Typed payload reads and the deny envelope                        |
| `japanese.py`      | Japanese detection and its threshold                             |
| `mirror_prose.py`  | Detection of a `.ja/` file whose Japanese is gone                |
| `rust_target.py`   | Cargo workspace root resolution and clippy output shaping        |
| `textlint.py`      | textlint config resolution and invocation                        |
| `hook_harness.py`  | Hook invocation from tests, with the exit status checked         |

## Skill contract

### Frontmatter

The frontmatter of `SKILL.md` decides when a skill starts and what it may touch. A tool missing from `allowed-tools` cannot be called even when the body says to call it.

| Key             | Role                                                          | When omitted               |
| --------------- | ------------------------------------------------------------- | -------------------------- |
| `name`          | Invocation name. `/name` and the Skill tool argument match it  | Required                   |
| `description`   | The material for the decision to invoke. The model sees this sentence alone | Required         |
| `allowed-tools` | The tools permitted during the run                            | Inherits the caller's set  |
| `model`         | The model the skill runs on                                   | The session model          |
| `argument-hint` | The argument shape shown to the user                          | No arguments               |
| `context`       | `fork` runs it in a separate context, leaving this one clean   | This context              |
| `agent`         | The matching agent name, carried by the reviewer skills       | None                       |
| `user-invocable`| `false` keeps it out of the slash-command list                | `true`                     |

### Supporting assets

A skill's subdirectories take their role from their name.

| Directory     | Contents                                              |
| ------------- | ----------------------------------------------------- |
| `scripts/`    | Deterministic scripts the skill calls through Bash    |
| `templates/`  | Skeletons for what it produces, such as issue bodies  |
| `references/` | Supplementary knowledge the body links to             |
| `tests/`      | Tests binding the skill body and its scripts          |

### Catalog

A skill marked "no" under user-invocable is a knowledge module the model loads from context.

| Skill                              | Model   | User-invocable | Artifact or side effect                             |
| ---------------------------------- | ------- | -------------- | --------------------------------------------------- |
| `census`                           | opus    | yes            | Candidate list of decisions with no DR              |
| `challenge`                        | opus    | yes            | A GO / NO-GO verdict with its counterarguments      |
| `checkout`                         | haiku   | yes            | A new git branch                                    |
| `commit`                           | haiku   | yes            | A git commit                                        |
| `dr`                               | opus    | yes            | A MADR v4 file under `docs/decisions/`              |
| `fix`                              | opus    | yes            | Edits confined to 1 to 3 files                      |
| `issue`                            | opus    | yes            | A GitHub issue                                      |
| `outcome`                          | opus    | yes            | `.claude/OUTCOME.md` created or updated             |
| `pr`                               | opus    | yes            | A draft pull request                                |
| `preview`                          | opus    | yes            | The match between a PR diff and its Plan            |
| `qualify`                          | opus    | yes            | build-ready / needs-plan / needs-fix / needs-split  |
| `research`                         | opus    | yes            | Findings, produced in a fork context                |
| `scribe`                           | inherit | yes            | A PR proposing entries under `docs/wiki/`           |
| `slice`                            | opus    | yes            | GitHub issues filed in dependency order             |
| `think`                            | opus    | yes            | A structured plan with units and test_command       |
| `transcribe`                       | inherit | yes            | A spreadsheet converted to Markdown                 |
| `use-cli-codegraph`                | inherit | no             | Knowledge of how to call codegraph                  |
| `use-cli-gcloud`                   | inherit | no             | Knowledge of how to call gsheet and gdoc            |
| `use-cli-recall`                   | inherit | no             | Knowledge of how to call recall                     |
| `use-cli-scout`                    | inherit | no             | Knowledge of how to call scout                      |
| `use-context-reviewer-readability` | inherit | no             | Starts `reviewer-readability` in a fork             |
| `use-context-reviewer-security`    | inherit | no             | Starts `reviewer-security` in a fork                |
| `use-context-reviewer-silence`     | inherit | no             | Starts `reviewer-silence` in a fork                 |
| `use-context-reviewer-testability` | inherit | no             | Starts `reviewer-testability` in a fork             |
| `use-context-root-cause-analysis`  | inherit | no             | The procedure for root cause analysis by elimination |
| `use-workflow-pageshot`            | opus    | no             | The path to a screenshot or a video                 |
| `use-workflow-tdd-cycle`           | inherit | no             | The RGRC cycle procedure                            |

## Agent contract

### Frontmatter and output

An agent always runs in a fork and carries none of the caller's conversation. Only the tools listed in `tools` are available, and a parenthesized entry such as `Bash(git:*)` narrows down to the subcommand.

| Key           | Role                                             |
| ------------- | ------------------------------------------------ |
| `name`        | The name passed as the Agent tool's `subagent_type` |
| `description` | The material for the decision to invoke          |
| `tools`       | The tools permitted                              |
| `model`       | The model it runs on                             |

An agent that returns findings follows the Base Fields in `agents/_lib/finding-schema.md`. The Agent row is filled by the caller, so a reviewer never writes its own name.

| Field          | Contents                                      |
| -------------- | --------------------------------------------- |
| `Severity`     | critical / high / medium / low                |
| `Category`     | Domain-specific category                      |
| `Location`     | `file:line`                                   |
| `Evidence`     | Code snippet or observation                   |
| `Trigger`      | A reproducible firing condition               |
| `Reasoning`    | Why it is a problem                           |
| `Fix`          | Suggested fix                                 |
| `Verification` | Check type and question                       |

### Catalog

There are 18 reviewers. The ROUTING table in `audit` picks 15 of them by file extension, and `causation`, `readability`, and `conformance` are called from a skill or from `build` directly. `agents/_lib/` holds no agent: it carries the finding format (`finding-schema.md`) and the severity calibration examples (`calibration-examples.md`).

| Category   | Agent                                                                                                                   | Model                        |
| ---------- | ----------------------------------------------------------------------------------------------------------------------- | ---------------------------- |
| critics    | `critic-audit`, `critic-design`, `critic-evidence`                                                                      | opus                         |
| enhancers  | `enhancer-code`, `enhancer-evidence`, `enhancer-integration`                                                            | opus                         |
| explorers  | `explorer-feature`                                                                                                      | opus                         |
| generators | `generator-snapshot`, `generator-test`                                                                                  | sonnet / opus                |
| resolvers  | `resolver-build`                                                                                                        | opus                         |
| reviewers  | `reviewer-accessibility`, `reviewer-causation`, `reviewer-conformance`, `reviewer-coverage`, `reviewer-design`, `reviewer-duplication`, `reviewer-readability`, `reviewer-react-pattern`, `reviewer-rust`, `reviewer-security`, `reviewer-silence`, `reviewer-testability` | opus |
| reviewers  | `reviewer-efficiency`, `reviewer-operations`, `reviewer-progressive`, `reviewer-prompt`, `reviewer-resilience`, `reviewer-reuse` | sonnet |

## Workflow contract

### Invocation and arguments

A workflow starts as `Workflow({name, args})`. args arrives as an object, a JSON string, or a bare string, and each script's `parseArgs` normalizes it to one shape. A bare string is the per-workflow shorthand: scope for `audit`, `polish`, and `assert`, and a DR directory or id list for `adrift`.

`repo` is required by every workflow. A call without an absolute path returns `{stopped: "no-repo"}`, so none of them run against the wrong repository.

| Workflow | args                                                     | Phases                                                              | stopped                                                        |
| -------- | -------------------------------------------------------- | ------------------------------------------------------------------- | -------------------------------------------------------------- |
| `build`  | `{issue, repo, base?}`                                   | Load / Revalidate / Branch / Code / Cleanup / Verify / Ship         | 14 codes (table below)                                         |
| `code`   | `{plan, repo, model?, commit?, issue?, untracked_baseline?}` | Implement / Verify                                               | `no-plan`, `no-repo`                                           |
| `audit`  | `{scope?, focus?, repo, noLimit?, skipPreflight?}`        | Pre-flight / Route / Review / Challenge / Verify / Integrate / Snapshot | `no-repo`                                                  |
| `polish` | `{scope?, repo, mode?, base?}`                           | Review / Challenge / Fix / Rejudge / Cleanup                        | `no-repo`                                                      |
| `assert` | `{scope?, base?, repo}`                                  | Bootstrap / Evidence / Challenge / Triage / Synthesize / Cleanup    | `no-repo`, `no-changes`, `codex-missing`                       |
| `shake`  | `{scope?, base?, repo}`                                  | Route / Shake / Fix                                                 | `no-repo`, `no-targets`                                        |
| `adrift` | `{dir?, repo, focus?}`                                   | Detect / Scan / Report                                              | `no-repo`, `no-drs`, `no-matching-drs`                         |

The stop codes of `build` split in two by whether a better `## Plan` on the issue would have prevented the stop. The script holds that split as `PLAN_QUALITY` and records one jsonl row per run.

| Group                          | Codes                                                                                        |
| ------------------------------ | --------------------------------------------------------------------------------------------- |
| A better Plan prevents it      | `no-plan`, `extraction-failed`, `invalid-plan`, `extraction-mismatch`, `oversized-unit`, `plan-drift` |
| Unrelated to the Plan          | `no-issue`, `no-repo`, `invalid-base`, `no-issue-body`, `dirty-branch-point`, `revalidate-failed`, `revalidate-incomplete`, `code-failed` |

### Return values

The return value is the only primary channel the caller reads. Even a workflow that writes a report file puts the numbers needed for a decision here.

| Workflow | Main keys                                                                                         |
| -------- | ------------------------------------------------------------------------------------------------- |
| `build`  | `pr_url`, `committed`, `unstaged`, `conformance_status`, `conformance_high`, `missing_tests`, `untouched_plan_files`, `backlog_candidates` |
| `code`   | `completed`, `skipped`, `anomalies`, `commits`, `verification`, `tests_pass`, `gates_pass`        |
| `audit`  | `findings`, `survivors`, `needs_context`, `tally`, `assignments`, `zero_reviewer_files`, `snapshot` |
| `polish` | `findings`, `survivors`, `fixed`, `reopened`, `needs_context`, `cleanup`                          |
| `assert` | `gate`, `gate_reason`, `build`, `tests`, `issues`, `root_causes`, `outcome_ref`, `report`         |
| `shake`  | `ecosystem`, `runs_per_dimension`, `targets`, `dropped`, `blockers`                               |
| `adrift` | `report_path`, `report_written`, `findings`, `priorities`, `unverifiable`, `external_refs`, `followup_candidates` |

### What the script decides

The OUTCOME statement about moving quality assurance off LLM discretion and onto a deterministic layer becomes concrete in this column. None of these judgments is reported by an agent; the script computes each one.

| Workflow | Judgment the script owns                                                                |
| -------- | ---------------------------------------------------------------------------------------- |
| `audit`  | The file-glob to reviewer assignment table                                              |
| `polish` | The confirmed / disputed / downgraded / needs_context triage, and the post-fix rejudge  |
| `shake`  | 10 runs per each of 4 dimensions, and the confirmed-flaky / latent-flaky / stable verdict |
| `assert` | The Ready / Ready (caveat) / NotReady gate, computed from build, tests, and issues       |
| `adrift` | Exhaustive per-DR listing, reviewer routing, and the file:line merge                    |
| `build`  | Validation of the extracted plan, the id cross-check, and the stop code                 |
| `code`   | The Red confirmation per unit, and routing a test-less unit to direct implementation    |

### Nesting

A workflow can nest another one level deep. That is how a routing table is reused instead of copied. Heavy assurance (`audit`, `polish`) is started by a person against the draft PR.

| Caller     | Nested workflow  | Purpose                                    |
| ---------- | ---------------- | ------------------------------------------ |
| `build`    | `code`           | TDD implementation per unit                |
| `assert`   | `audit`          | Collection of static evidence              |

## Quality gates

### Local

External binaries run right after an edit and after a Bash call. `gates` has a 120-second budget, looking at the whole diff on `Write` / `Edit` and at the changed files alone as `gates changed` after Bash.

### CI

`.github/workflows/test.yml` runs on push to main and on pull requests. It has one job, whose steps run in this order. Python discovery is a `find` rather than unittest discovery, because the test directories are not packages and discovery cannot recurse from the root. ruff and rumdl are absent from the JS dependencies, so their versions are pinned directly.

| Step          | Command                                                                                      |
| ------------- | -------------------------------------------------------------------------------------------- |
| Node tests    | `node --test` over `tests/`, `agents/**/tests/`, `hooks/**/tests/`, `skills/**/tests/`, `workflows/**/tests/` |
| Python tests  | `find agents hooks skills workflows -name '*_test.py'`, one file per invocation               |
| Shell tests   | `find hooks -name '*.test.sh'`, one file per bash invocation                                  |
| oxlint        | `npx oxlint`                                                                                  |
| textlint      | `npx textlint '.ja/**/*.md'`                                                                  |
| ruff          | `pipx run --spec ruff==0.16.4 ruff check .`                                                   |
| ruff format   | `pipx run --spec ruff==0.16.4 ruff format --check .`                                          |
| rumdl         | `pipx run --spec rumdl==0.2.58 rumdl check .`                                                 |

### Configuration files

The lint targets and thresholds live in configuration files. This document does not restate their numbers.

| File                  | What it decides                                                         |
| --------------------- | ----------------------------------------------------------------------- |
| `.textlintrc.json`    | Japanese sentence-length cap, comma cap, dearu unification, spacing between half and full width |
| `.rumdl.toml`         | Which Markdown rules are disabled                                       |
| `.oxlintrc.json`      | JavaScript static checks                                                |
| `.guardrails.json`    | The guardrails rules, and the exemption for deliberately vulnerable fixtures |
| `ruff.toml`           | Python checks and formatting                                            |
| `pyrightconfig.json`  | Python type checking                                                    |
| `knip.json`           | Unused dependency detection                                             |

### Cross-cutting tests

`tests/` binds conventions in code. What a document alone failed to hold ended up here.

| Test                       | Convention it binds                                             |
| -------------------------- | --------------------------------------------------------------- |
| `decision-records.test.js` | No duplicate DR number, and a title heading matching its filename |
| `live-instructions.test.js`| Wording in instructions: the retired name ADR, home-anchored paths, un-narrowed Bash grants, a workspace path missing `.claude/` |
| `manual-lane.test.js`      | Each manual-lane skill maps to a build stage, and a shared rule names both readers |
| `phase-handoff.test.js`    | A Phase that hands something forward is named back by the receiving Phase |
| `prose-language.test.js`   | Test names on the English side are not Japanese                 |
| `skill-h1.test.js`         | A wrapper skill's H1 is its own name, and a user-invocable skill's H1 opens with its slash command |
| `table-paragraph.test.js`  | No paragraph directly below a table                             |

## Distribution contract

`.claude-plugin/marketplace.json` is a single-plugin configuration. Installing `build` clones the whole repository once, and every skill, agent, and workflow loads under the `build:` namespace.

| Item           | Value                       |
| -------------- | --------------------------- |
| Marketplace    | `thkt-development-workflows`|
| Plugin         | `build` only                |
| version        | 4.1.0                       |
| source         | github `thkt/dotclaude`     |

## Mirror contract

`.ja/` is canonical, and the English side is mirrored in the same commit. [MIRROR.md](../rules/conventions/MIRROR.md) carries the details.

| Target                          | Treatment                                                  |
| ------------------------------- | ---------------------------------------------------------- |
| File carrying prose             | Translate the prose alone; identifiers and schemas stay identical |
| File carrying no prose          | Identical copy                                             |
| `output-styles/**`              | No mirror. One Japanese file at the real path              |
| Tests                           | English side only                                          |

## What to touch when adding

Here are the files a new component touches, and the command that confirms the addition took effect.

| Adding   | Files to touch                                                          | Confirmation                                |
| -------- | ----------------------------------------------------------------------- | ------------------------------------------- |
| A hook   | `hooks/<event>/`, `settings.json`, `hooks/<event>/tests/`               | `find hooks -name '*_test.py'` and `find hooks -name '*.test.sh'` |
| A skill  | `skills/<name>/SKILL.md`, `skills/<name>/tests/`, `.ja/skills/<name>/`  | `node --test "skills/**/tests/*.test.js"`   |
| An agent | `agents/<category>/<name>.md`, `.ja/agents/<category>/`                 | `node --test "agents/**/tests/*.test.js"`   |
| A workflow | `workflows/<name>.js`, `workflows/tests/`, `.ja/workflows/`           | `node --test "workflows/**/tests/*.test.js"`|
| A DR     | `docs/decisions/NNNN-*.md`                                              | `node --test tests/decision-records.test.js`|
| A document | `.ja/docs/<NAME>.md` first, `docs/<NAME>.md` in the same commit       | `npx textlint '.ja/**/*.md'` and `rumdl check .` |
