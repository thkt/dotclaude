---
globs: ["**/tests/**/*", "**/test/**/*"]
scenes: []
---

# 空振りするテストと対象の消えたテストは理由を残して消す

## 内容

実装を壊しても落ちないテストと、検査対象が退役したテストは、消す理由をコミットに残して消す。残すと本数だけが守りに見え、退行を通す。散文を grep で照合するだけのテストは、文言を書き換えると同時に期待値も書き換わるので、この形になりやすい。何を壊したときに落ちるはずかを言えないテストが対象になる。

判定は `rules/development/TESTING.md` の Whether the test swings at nothing が持つ。ここが持つのは、空振りと分かった後の扱い。

## 定型手順

1. 対象のテストが守っているものを 1 文で言う。言えなければ空振りを疑う
2. 実装側をその主張が壊れる形で壊し、テストが落ちるか確かめる
3. 落ちなければ、主張を捉える形へ書き直すか、消す
4. 消すときは、何を守っていたつもりで、なぜ守れていなかったかをコミット本文に書く
5. 検査対象が退役して消す場合は、退役した対象の名前を書く

## 参照コード

- `rules/development/TESTING.md` の Whether the test swings at nothing (空振りする 3 つの型)
- `rules/conventions/SKILL_REFACTOR.md` の Stage 7 Pinning (壊して落ちることの確認を段階として置く)

## 根拠

- #166 対象が消えたまま残っていたテストを消した
- #167 grep 照合だけのテストが実装の退行を通していた
- #174 同じ形のテストをまとめて整理した
- #180 消す理由を記録する形に揃えた
- #389 本文をフォームのラベルから組んで同じフォームと照合していたテストは、ラベルを改名しても両側が同時に変わるので 3 通り壊しても通った。qualify の「表が 8 行を超える」を求めるテストは、無関係な行の退役で落ちたので判定ごと外した
- (research) 監査で 3 つの形を確認した。`test()` を 1 本も持たない comment 専用の test ファイル、T-001 の陽性対照と同一の assertion しか持たない T-002、round-trip を名乗りながら `strictify` の結果を捨てて元の schema で続けるテスト
