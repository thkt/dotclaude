---
globs: ["**/tests/**/*", "**/test/**/*"]
scenes: []
---

# 不在を検査するテストは陽性対照 (検査を確実に落とす fixture) を常設しないと、対象が壊れても気づかず通過する

## 内容

不在・0 件を確認するテストは、検査そのものが壊れて何も検出しなくなっても green のまま通ってしまう。意図的に違反を注入した陽性対照を同じテストの中に常設し、その陽性対照から検査の手掛かり (識別子・パターン) を取り除いた写しに対しては検出漏れとして落ちることを確かめておく。パターンリストで判定する検査は、リストに無い形の違反も陽性対照でカバーできているか確かめる。

## 定型手順

1. 不在・0 件を検査するテストを書くとき、検査が確実に検出できるはずの違反を注入した陽性対照を用意する
2. 陽性対照から検査の手掛かりを取り除いた写しに対して同じ検査を走らせ、検出漏れとして落ちることを確認する
3. パターンリストで判定する検査は、リストに無い種類の違反も陽性対照として試す
4. 陽性対照の fixture は、検査が探す定数から組み立てず literal で書く。定数から組むと typo が fixture 側にも流れ、検査が壊れていても対照が通る

## 参照コード

- `skills/scribe/tests/structure-page.test.ts` の T-229（参照コードが名指す定数を改名した写しでは検査が対象を見つけられなくなることを確認する）

## 根拠

- #571 `_unchecked_claim_rows` の陽性対照テストを追加し、検査を外すと同じ行が検査漏れとして落ちることを確認した
- (research) 構造ページの主張検証プロトタイプで、`E no-io` 検査が `child_process` import の混入後も陽性対照なしでは PASS のまま通過することを確認した
- (research) retirement テスト 2 本の陽性対照が探す定数 (`RETIRED_PATH`) から fixture を組み立てていて、定数を `_lib/gate-XXXX.py` に書き換えても対照が通ることを変異で確認した
