---
globs: ["**/tests/**/*", "**/test/**/*"]
scenes: []
---

# 同じ入力を両側でフィルタしてから件数だけを比較するテストは、実質的な差があっても一致してしまう。集合の内容で比較する

## 内容

二つの集合を比較するテストが、比較の前に同じ条件で両側をフィルタしていると、フィルタが両側を同時に動かすため、要素が抜け落ちても件数だけは揃って一致してしまう。差を検出するには、フィルタ後の件数ではなく集合の内容 (要素そのもの) を比較する。

## 定型手順

1. 二つの集合を比較するテストが、比較前に同じ条件で両側をフィルタしていないか確認する
2. フィルタが両側を同時に動かす形なら、件数比較を集合の内容 (要素そのもの) の比較に置き換える
3. 置き換え後、片側から要素を 1 つ落として本当に検出できるか確かめる

## 参照コード

- `skills/scribe/tests/structure-page.test.ts` の T-227（ページ集合を件数ではなく名前の集合として突き合わせる）

## 根拠

- #571 独立レビューで、境界セクションの主張が契約/要求の unchecked 判定から漏れていることが指摘され、`_unchecked_claim_rows` が件数でなく行そのものを返す設計であることが確認された
- (research) 構造ページの主張検証プロトタイプで、`A names` 検査がページの宣言名をファイル存在でフィルタしてから件数だけを比較するため、workflow 1 本の削除で両側が同時に動き検出漏れになることを確認した
