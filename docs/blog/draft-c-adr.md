# ADR を棚に置かず「生きているドキュメント（Living Documentation）」として運用する

こんにちは。Gaji-Labo でフロントエンドエンジニアをしている thkt です。

普段はチームの一員としてプロダクト開発を進めていますが、個人では AI エージェントのために使える小さな CLI ツールを作ったりしています。

先日、そのひとつで ADR（Architecture Decision Record）を書いていたら、思いがけずコードのバグを発見しました。

ADR は、重要な意思決定を背景や理由を含めて残すものです。しかし、運用ルールが曖昧だと、残しただけでそれきり読まれない文書になりがちです。今回は、このバグに気づいた経緯から考えた「ADR を生きているドキュメント（Living Documentation）として運用する」ことの良さについて話をしたいと思います。

## 書こうとして分類ミスに気づいた話

作っていた CLI には、外部 API を叩いてエラーを受け取ると、呼び出し側のスクリプトに「もう一度試すべきか、停止すべきか」を伝える機能を実装していました。一時的な失敗なら再試行、使い方の誤りなら停止という一般的な分岐です。

この「API エラーの振り分け」の実装について判断が記録されていなかったので、改めて ADR として記録を残すことにしました。

実装では**HTTP の 401/403 を停止に分類**していたのでそのように書こうと思ったのですが、その時に API の課金上限の枯渇でも 403 が返ってくるパターンがあることに気づきました。その場合、時間を置けば回復する可能性のある一時的な失敗に分類できます。

これは、改めて実装を文書化するなかで、書く内容を決まった事実ではなく「確かめるべき仮説」として読むことで発見できた事柄でした。

## 「決定を記録する」だけだと、ADR は棚に置かれる

こんなふうに、ADR は書く瞬間に実装を見直すきっかけをくれますが、それで役目を終えてしまうことも少なくありません。

ADR は「決定の文脈と理由を記録する文書」なので、記録だけを主眼に置くと見返されず、数ヶ月後にコードと食い違っていても気づかなかったということが起こりえます。

そうならないためには、**書いた内容が実態と合っているかを確かめ続けている**必要があります。

## AI エージェントを活用して Notes で実態に合わせ続ける

では、どうすれば確かめ続けられるのでしょうか。

ドキュメントは増えれば増えるほど確認のコストが膨らむので、ADR にする内容を絞ることからはじめます。私の場合は、型やテスト、linter などで自動的に気づけない判断だけを ADR に残すようにしています。

残した ADR と実装を照らし合わせる作業は、AI エージェントに任せています。私はレビューを行うスキルに ADR とのギャップがないかを確認するように仕込んで、決定とのズレがないかを確かめさせています。

クライアント名や設定値といった具体的なシンボルで ADR を書いておくことで、それがそのまま**コードと突き合わせるチェックリスト**になるというわけです。

ADR の更新には `Notes` を使用します。ADR 自体は合意した決定の足跡として変更しないのがルールなので、注釈のような形で変更を重ねます。根本から切り替わるような決定であれば新たに ADR を起票して、そちらが正であることを `Notes` に残す運用にしています。

たとえば、あるツールに「エラーの種類をどう分類するか」を決めた ADR があります。最初に実装したときは 8 種類でしたが、その後も機能を足したり監査でズレを見つけたりするたびに増えていき、いまは 13 種類です。それでも ADR 本文の決定は一度も書き換えていません。増えていった経緯は、すべて日付つきで `Notes` に積み上げています。

```markdown
> **Notes (2026-05-13)**: 当初挙げた `ReadmeMissing` は、README が無い場合（404）を意図的に何もしない扱いにしていたため、分類には含めなかった。README まわりは失敗のしかたで 3 つに分割した。
```

人間が決定をまとめ、AI が実装のたびに照らし合わせる。この分担にすると ADR を確かめ続ける負担が小さくて済みます。

## まずは書き始めること

ADR を生きているドキュメントにするには、まずは書くことが大事です。書いたあとは AI に比較を任せて `Notes` を更新していくというのが、決定に即したコードとドキュメントを維持する運用として無理がないのではないかと思っています。

ぜひ AI エージェントを活用して ADR を生きているドキュメントにする運用を試してみてください！

<!--
CTA 節 (公開時に Gaji-Labo house style へ調整。サービス/採用導線はテンプレなので実リンクを差し込む):
#### Gaji-Labo では一緒に開発するエンジニアを募集しています
... 開発プロセスや品質の作り込みに関心のある方向けの導線 ...
-->

---

<!--
出典メモ (公開時は削除):
- 一次ソース: session 5f7e4857-7d70-4be5-82ea-dd590c2156cb (project: scout, /Users/thkt/GitHub/cli/scout)。jsonl 健在 (6.3MB, 3030 行)。
- 第11版 (2026-06-18): L41-44 の Note 作例を、ユーザー提供の実 ADR Notes 履歴 (degraded/DegradedReason、scout) で差し替え。生きているドキュメントの証拠が「主張」だけだった弱点を補う狙い。叙述の背骨は variant 数の弧 = 実装時 8 (Note 2, 2026-05-13) → 最終 13 (Note 5, 2026-06-17)。引用は 1 本に絞り、Note 2 の `ReadmeMissing` 見送り (404 を意図的に silent 扱い→分類化せず、README を 3 分割) を平易化して採用。Note 5 (DecodeUncertain: chardetng/reliability gate/from_utf8_lossy/exit code) は jargon 密度が高く非専門読者に不適なため逐語引用は避けた。※注意: 前版までの「当初 2 種」は作例の未確認数字で実データにない。実データで裏が取れる弧は 8→13 のみ (Note 1 の degraded:bool は bool フィールド 1 個で variant 数ではない)。本文不変・Notes 追記の構図は実履歴と一致。第7版メモの「variant 2→3」作例は本版で実データ置換により解消。
- 第10版 (2026-06-18): ユーザー大幅改稿後の骨組み (実例以降がエッセンスのみ) を散文化。流れ=書いて発見→放置されがち→棚に置かない運用 (絞る+Note)→AI に任せる→締め。(1) L27 の途切れた文と「運用ルール」重複を解消。(2) 浮いていた一文と薄い「## AI に任せる」を「## 照らし合わせを、AI エージェントに任せる」節に統合し、人間が書き AI が照合する分担として展開。(3) 締めから、削除済み節を参照する「シンボルや Confirmation 欄」を外し、3 本柱 (書く瞬間/Note/AI) に整合。(4) 実例節末との「確かめるべき仮説」重複を橋渡し文に置換。※「AI に確かめさせている」は著者の運用記述。実配線 (hook/agent で ADR 照合が動いているか) を公開前に確認すること。fetch_http 等の具体シンボルは作例。事実・数値・ヘッジ不変。
- 第9版 (2026-06-18): 文体の印象を thkt 記事 3 本に全体反映。(1) 実例節に自己反省ビート「2 件とも、Decision を書き出すまでまったく気づいていませんでした」を追加 (「目から鱗でした」型の素直な驚き)。(2) 最終節に温かい内省的締め「大げさな仕組みは要りません。〜変わるのではないかと思っています」を追加 (house の「〜のではないでしょうか」型クロージング)。本文の事実・ヘッジは不変。
- 第8版 (2026-06-18): 著者 thkt 自身の Gaji-Labo 記事 3 本 (24253/23934/22547) の文体を反映。冒頭を house の署名挨拶「こんにちは。Gaji-Labo でフロントエンドエンジニアをしている thkt です。」に統一し、業務 (チーム開発)→個人 (CLI ツール) の橋渡し一文、読者への問いかけ (「書いたけれど読まれない文書にした経験はないか」)、「今回は〜紹介したいと思います」の予告を追加。本文は既にユーザー共同編集済みのため intro のみ調整。
- 第7版 (2026-06-18): 「照らし合わせる」の連発を圧縮。リード/各節冒頭を「確認/照合/突き合わせる/実態と合っているか」に分散し、キーワード「照らし合わせ続ける」は最終節タイトル+本文の2回に温存。「具体的に書くほど〜」節を動作ベース (grep/コードを開く/読む) に書き換え。「Note で〜」節に決定後の追記実例 (variant 2→3) を追加し本体側の証拠を補強 (※実際の追記内容と要照合)。最終節を圧縮。throat-clearing と自己実況を言い切りに。内輪固有名 (critic-design/scout) を一般化、「副産物」連発を解消。
- 第6版 (2026-06-18): 「自分の過去判断も、照らす対象にする」節を全削除 (公式 doc を読むのは当たり前で芯から外れる、というユーザー判断)。Bash permission matcher の例も合わせて削除。おわりにの「別の記事で」予告は独立して成立するため影響なし。
- 第5版 (2026-06-18): 実例セクションを非専門読者向けに書き換え。内部型名 (ErrorCode/TempFailure/UsageError)、exit code (64/75)、fail-fast/abuse detection を落とし、「再試行していい / 諦めろ」という人間に分かる軸を前面に。確定2件 + 横展開・リポジトリ未記載の hedge は不変。
- 第4版 (2026-06-18): 冗長表現の刈り込み。文末ヘッジ (〜と感じています/思っています/考えています) を 1 箇所 (「多いと思います」) を除き plain です/でした 化。「行為」「こと」の重複、「〜してみました」の連発、再掲の言い回しを除去。第3版 約3700字 → 約3400字。
- 第3版: 参照記事の本文文字数 (24910=約2200字 / 24747 TSKaigi=約3300字) に合わせ第2版 約4570字から圧縮。おわりにの2局面再掲リストを散文化、実例叙述短縮。
- 文体/構成: Gaji-Labo ブログ house style (です・ます調 + 一人称 + 太字リード箇条書き + 温かいおわりに + 末尾 CTA)。表は bold-lead 箇条書きに変換済み。
- 主題: 「ADR を living document として運用する」軸。検証の2局面 (書く瞬間/決定後) を本体として統一。旧「バグは ADR にしない」独立節は廃し scope 規律を Note 運用節に吸収。
- 数値: 原典で裏付くのは「25 候補 → 4 本起票」(Wave 1) のみ。率(%)は使わず実数のみ。「rejection 率 16-29%」は原典になく不使用。
- critic-design は原典の逐語でなくパラフレーズのため地の文の要約に。要点: 「ADR に値するのは将来 型・テスト・lint で気づけない判断のみ。外部契約とセキュリティ境界の 2 カテゴリに限定」「bug を ADR にするな。fix commit + テストで永続化」。fetch_http の blockquote は ADR 本文の作例引用のため維持。
- バグ: Gemini 403 分類と GitHub 403 ヘッダの 2 件は scout セッション内で確定。横展開は連想で所属リポジトリ未確定。Slack 等の固有リポジトリ名は未確定のため出していない。確定 2 件 + パターン横展開という表現を維持。
- variant 2→3 の Note 追記例 (第7版で追加) は原典未照合の作例。公開前に実際の追記内容と突き合わせること。
- `fetch_http` / `Policy::none()` の ADR 例文は説明用の作例。
-->
