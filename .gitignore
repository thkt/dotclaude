# OS
.DS_Store
Thumbs.db

# Editors
.antigravitycli/
.cursor/
.idea/
.vscode/
.zed/
*.swo
*.swp
*~

# Workspace / temp
archive/
debug/
node_modules/
workspace/
history/
*.log
*.temp
*.tmp

# Claude
/.analysis/
/.claude/*
!/.claude/rules/
!/.claude/OUTCOME.md
!/.claude/workspace/
/.claude/workspace/*
!/.claude/workspace/research/
/.claude/workspace/research/*
!/.claude/workspace/research/*.md
/.credentials.json
/agent-memory/
/chrome/
/herdr-auto-title/
/ide/
# このリポジトリは public。memory/ に業務文脈が、jsonl に全会話が入るため打ち消さない。
/projects/
/shell-snapshots/
/sounds/
/statsig/
/tasks/
/teams/
/telemetry/
/todos/

# Session history
file-history/
*.jsonl

# Private / local config
local/
.env
.env.*
!.env.example
!.env.sample
*.local
*.local.*

# Secrets / credentials
credentials.csv
credentials.json
id_ed25519
id_rsa
token.json
*-service-account*.json
*.key
*.p12
*.pem
*.pfx
*.secret
*.secrets

# Database dumps
dump.sql
*.sql.gz
*.sqlite
*.sqlite3

# MCP data
.mcp.json
.playwright-cli/
mcp-needs-auth-cache.json

# CodeGraph
# codegraph init は .codegraph/.gitignore も置くが、無視の指定はここだけに持たせる
.codegraph/

# Python
__pycache__/
*.pyc

# Cache
stats-cache.json
# ディレクトリに限る。末尾が cache のソースファイルまで飲まないため
*cache/

# Runtime
logs/
plans/
session-env/
sessions/

# Usage / analytics
usage-data/

# Plugins
.claude-plugin/.cache/
plugins/*
!plugins/README.md
# claude.ai アカウントから同期されるスキル。ここでの編集はアカウントに保存されない
skills/synced/

# Local state
BACKLOG.md
.cc-writes/
.last-cleanup
.last-update-result.json

# Backups
backups/

# Job / daemon runtime state
jobs/
daemon/
daemon.lock
daemon.status.json
gh-pr-status-cache.json

# グローバル gitignore の build/ が workflows/build/ をソースごと除外するため打ち消す
!workflows/build/
!.ja/workflows/build/
